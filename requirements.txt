# REAPER MCP Server Dependencies

# MCP protocol implementation
# Capped below 2.0 -- 2.x removed `mcp.server.fastmcp`, which this server imports.
mcp>=1.28.1,<2

# Async HTTP client for communicating with REAPER
httpx>=0.25.0

# Transitive, via mcp -> PyJWT. Floored for PYSEC-2026-3552; nothing in the
# direct tree constrains it, so the resolver otherwise picks 49.0.0.
cryptography>=50.0.0
